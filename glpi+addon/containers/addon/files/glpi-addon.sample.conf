[logging]
# Logging level: change to DEBUG for more information, INFO for less information
level = DEBUG
# level = INFO

[glpi]
# URL of GLPI server
url = http://glpi
apptoken = YOURAPPTOKEN
auth = YOURUSERTOKEN

[listen]
# if changed from default, docker compose override must override the default value in docker-compose.yml
# port = 5000
