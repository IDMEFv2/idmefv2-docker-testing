[logging]
# Logging level: change to DEBUG for more information, INFO for less information
level = DEBUG
# level = INFO

[glpi]
# URL of GLPI server
url = http://glpi
apptoken = YOURAPPTOKEN
auth = YOURUSERTOKEN

[listen]
# if changed from default, docker redirection ports must be defined in .env file
# port = 5000
